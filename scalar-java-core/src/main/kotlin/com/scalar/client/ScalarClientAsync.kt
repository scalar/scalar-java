// File generated by Scalar SDK Generator. DO NOT EDIT.

package com.scalar.client

import com.scalar.core.ClientOptions
import com.scalar.services.async.AuthenticationServiceAsync
import com.scalar.services.async.LoginPortalServiceAsync
import com.scalar.services.async.NamespaceServiceAsync
import com.scalar.services.async.RegistryServiceAsync
import com.scalar.services.async.RuleServiceAsync
import com.scalar.services.async.ScalarDocServiceAsync
import com.scalar.services.async.SchemaServiceAsync
import com.scalar.services.async.TeamServiceAsync
import com.scalar.services.async.ThemeServiceAsync
import java.util.function.Consumer

/**
 * A client for interacting with the Scalar REST API asynchronously. You can also switch to
 * synchronous execution via the [sync] method.
 *
 * This client performs best when you create a single instance and reuse it for all interactions
 * with the REST API. This is because each client holds its own connection pool and thread pools.
 * Reusing connections and threads reduces latency and saves memory. The client also handles rate
 * limiting per client. This means that creating and using multiple instances at the same time will
 * not respect rate limits.
 *
 * The threads and connections that are held will be released automatically if they remain idle. But
 * if you are writing an application that needs to aggressively release unused resources, then you
 * may call [close].
 */
interface ScalarClientAsync {

    /**
     * Returns a version of this client that uses synchronous execution.
     *
     * The returned client shares its resources, like its connection pool and thread pools, with
     * this client.
     */
    fun sync(): ScalarClient

    /**
     * Returns a view of this service that provides access to raw HTTP responses for each method.
     */
    fun withRawResponse(): WithRawResponse

    /**
     * Returns a view of this service with the given option modifications applied.
     *
     * The original service is not modified.
     */
    fun withOptions(modifier: Consumer<ClientOptions.Builder>): ScalarClientAsync

    /** Registry */
    fun registry(): RegistryServiceAsync

    /** Schemas */
    fun schemas(): SchemaServiceAsync

    /** Login Portals */
    fun loginPortals(): LoginPortalServiceAsync

    /** Rules */
    fun rules(): RuleServiceAsync

    /** Themes */
    fun themes(): ThemeServiceAsync

    /** Teams */
    fun teams(): TeamServiceAsync

    /** Scalar Docs */
    fun scalarDocs(): ScalarDocServiceAsync

    /** Namespaces */
    fun namespaces(): NamespaceServiceAsync

    /** Authentication */
    fun authentication(): AuthenticationServiceAsync

    /**
     * Closes this client, relinquishing any underlying resources.
     *
     * This is purposefully not inherited from [AutoCloseable] because the client is long-lived and
     * usually should not be synchronously closed via try-with-resources.
     *
     * It's also usually not necessary to call this method at all. the default HTTP client
     * automatically releases threads and connections if they remain idle, but if you are writing an
     * application that needs to aggressively release unused resources, then you may call this
     * method.
     */
    fun close()

    /** A view of [ScalarClientAsync] that provides access to raw HTTP responses for each method. */
    interface WithRawResponse {

        /**
         * Returns a view of this service with the given option modifications applied.
         *
         * The original service is not modified.
         */
        fun withOptions(
            modifier: Consumer<ClientOptions.Builder>
        ): ScalarClientAsync.WithRawResponse

        /** Registry */
        fun registry(): RegistryServiceAsync.WithRawResponse

        /** Schemas */
        fun schemas(): SchemaServiceAsync.WithRawResponse

        /** Login Portals */
        fun loginPortals(): LoginPortalServiceAsync.WithRawResponse

        /** Rules */
        fun rules(): RuleServiceAsync.WithRawResponse

        /** Themes */
        fun themes(): ThemeServiceAsync.WithRawResponse

        /** Teams */
        fun teams(): TeamServiceAsync.WithRawResponse

        /** Scalar Docs */
        fun scalarDocs(): ScalarDocServiceAsync.WithRawResponse

        /** Namespaces */
        fun namespaces(): NamespaceServiceAsync.WithRawResponse

        /** Authentication */
        fun authentication(): AuthenticationServiceAsync.WithRawResponse
    }
}
