// File generated by Scalar SDK Generator. DO NOT EDIT.

package com.scalar.services.blocking

import com.scalar.core.ClientOptions
import com.scalar.core.RequestOptions
import com.scalar.core.checkRequired
import com.scalar.core.handlers.errorBodyHandler
import com.scalar.core.handlers.errorHandler
import com.scalar.core.handlers.jsonHandler
import com.scalar.core.http.HttpMethod
import com.scalar.core.http.HttpRequest
import com.scalar.core.http.HttpResponse
import com.scalar.core.http.HttpResponse.Handler
import com.scalar.core.http.HttpResponseFor
import com.scalar.core.http.json
import com.scalar.core.http.parseable
import com.scalar.core.prepare
import com.scalar.models.Uid
import com.scalar.models.schemas.SchemaCreateParams
import com.scalar.models.schemas.SchemaDeleteParams
import com.scalar.models.schemas.SchemaListParams
import com.scalar.models.schemas.SchemaListResponse
import com.scalar.models.schemas.SchemaUpdateParams
import com.scalar.services.blocking.schemas.AccessGroupService
import com.scalar.services.blocking.schemas.AccessGroupServiceImpl
import com.scalar.services.blocking.schemas.VersionService
import com.scalar.services.blocking.schemas.VersionServiceImpl
import java.util.function.Consumer
import kotlin.jvm.optionals.getOrNull

/** Schemas */
class SchemaServiceImpl internal constructor(private val clientOptions: ClientOptions) :
    SchemaService {

    private val withRawResponse: SchemaService.WithRawResponse by lazy {
        WithRawResponseImpl(clientOptions)
    }

    private val version: VersionService by lazy { VersionServiceImpl(clientOptions) }

    private val accessGroup: AccessGroupService by lazy { AccessGroupServiceImpl(clientOptions) }

    override fun withRawResponse(): SchemaService.WithRawResponse = withRawResponse

    override fun withOptions(modifier: Consumer<ClientOptions.Builder>): SchemaService =
        SchemaServiceImpl(clientOptions.toBuilder().apply(modifier::accept).build())

    /** Schemas */
    override fun version(): VersionService = version

    /** Schemas */
    override fun accessGroup(): AccessGroupService = accessGroup

    override fun create(params: SchemaCreateParams, requestOptions: RequestOptions): Uid =
        // post /v1/schemas/{namespace}
        withRawResponse().create(params, requestOptions).parse()

    override fun update(params: SchemaUpdateParams, requestOptions: RequestOptions): Any? =
        // patch /v1/schemas/{namespace}/{slug}
        withRawResponse().update(params, requestOptions).parse()

    override fun list(
        params: SchemaListParams,
        requestOptions: RequestOptions,
    ): List<SchemaListResponse> =
        // get /v1/schemas/{namespace}
        withRawResponse().list(params, requestOptions).parse()

    override fun delete(params: SchemaDeleteParams, requestOptions: RequestOptions): Any? =
        // delete /v1/schemas/{namespace}/{slug}
        withRawResponse().delete(params, requestOptions).parse()

    class WithRawResponseImpl internal constructor(private val clientOptions: ClientOptions) :
        SchemaService.WithRawResponse {

        private val errorHandler: Handler<HttpResponse> =
            errorHandler(errorBodyHandler(clientOptions.jsonMapper))

        private val version: VersionService.WithRawResponse by lazy {
            VersionServiceImpl.WithRawResponseImpl(clientOptions)
        }

        private val accessGroup: AccessGroupService.WithRawResponse by lazy {
            AccessGroupServiceImpl.WithRawResponseImpl(clientOptions)
        }

        override fun withOptions(
            modifier: Consumer<ClientOptions.Builder>
        ): SchemaService.WithRawResponse =
            SchemaServiceImpl.WithRawResponseImpl(
                clientOptions.toBuilder().apply(modifier::accept).build()
            )

        /** Schemas */
        override fun version(): VersionService.WithRawResponse = version

        /** Schemas */
        override fun accessGroup(): AccessGroupService.WithRawResponse = accessGroup

        private val createHandler: Handler<Uid> = jsonHandler<Uid>(clientOptions.jsonMapper)

        override fun create(
            params: SchemaCreateParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<Uid> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("namespace", params.namespace().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.POST)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "schemas", params._pathParam(0))
                    .body(json(clientOptions.jsonMapper, params._body()))
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response
                    .use { createHandler.handle(it) }
                    .also {
                        if (requestOptions.responseValidation!!) {
                            it.validate()
                        }
                    }
            }
        }

        private val updateHandler: Handler<Any?> = jsonHandler<Any?>(clientOptions.jsonMapper)

        override fun update(
            params: SchemaUpdateParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<Any?> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("slug", params.slug().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.PATCH)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "schemas", params._pathParam(0), params._pathParam(1))
                    .body(json(clientOptions.jsonMapper, params._body()))
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response.use { updateHandler.handle(it) }
            }
        }

        private val listHandler: Handler<List<SchemaListResponse>> =
            jsonHandler<List<SchemaListResponse>>(clientOptions.jsonMapper)

        override fun list(
            params: SchemaListParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<List<SchemaListResponse>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("namespace", params.namespace().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.GET)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "schemas", params._pathParam(0))
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response
                    .use { listHandler.handle(it) }
                    .also {
                        if (requestOptions.responseValidation!!) {
                            it.forEach { it.validate() }
                        }
                    }
            }
        }

        private val deleteHandler: Handler<Any?> = jsonHandler<Any?>(clientOptions.jsonMapper)

        override fun delete(
            params: SchemaDeleteParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<Any?> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("slug", params.slug().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.DELETE)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "schemas", params._pathParam(0), params._pathParam(1))
                    .apply { params._body().ifPresent { body(json(clientOptions.jsonMapper, it)) } }
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response.use { deleteHandler.handle(it) }
            }
        }
    }
}
