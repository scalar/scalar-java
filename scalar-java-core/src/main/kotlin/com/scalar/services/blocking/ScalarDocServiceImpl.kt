// File generated by Scalar SDK Generator. DO NOT EDIT.

package com.scalar.services.blocking

import com.scalar.core.ClientOptions
import com.scalar.core.RequestOptions
import com.scalar.core.checkRequired
import com.scalar.core.handlers.errorBodyHandler
import com.scalar.core.handlers.errorHandler
import com.scalar.core.handlers.jsonHandler
import com.scalar.core.http.HttpMethod
import com.scalar.core.http.HttpRequest
import com.scalar.core.http.HttpResponse
import com.scalar.core.http.HttpResponse.Handler
import com.scalar.core.http.HttpResponseFor
import com.scalar.core.http.json
import com.scalar.core.http.parseable
import com.scalar.core.prepare
import com.scalar.models.scalarDocs.ScalarDocCreateGuideParams
import com.scalar.models.scalarDocs.ScalarDocCreateGuideResponse
import com.scalar.models.scalarDocs.ScalarDocListGuidesParams
import com.scalar.models.scalarDocs.ScalarDocListGuidesResponse
import com.scalar.models.scalarDocs.ScalarDocPublishGuideParams
import com.scalar.models.scalarDocs.ScalarDocPublishGuideResponse
import java.util.function.Consumer
import kotlin.jvm.optionals.getOrNull

/** Scalar Docs */
class ScalarDocServiceImpl internal constructor(private val clientOptions: ClientOptions) :
    ScalarDocService {

    private val withRawResponse: ScalarDocService.WithRawResponse by lazy {
        WithRawResponseImpl(clientOptions)
    }

    override fun withRawResponse(): ScalarDocService.WithRawResponse = withRawResponse

    override fun withOptions(modifier: Consumer<ClientOptions.Builder>): ScalarDocService =
        ScalarDocServiceImpl(clientOptions.toBuilder().apply(modifier::accept).build())

    override fun createGuide(
        params: ScalarDocCreateGuideParams,
        requestOptions: RequestOptions,
    ): ScalarDocCreateGuideResponse =
        // post /v1/guides
        withRawResponse().createGuide(params, requestOptions).parse()

    override fun listGuides(
        params: ScalarDocListGuidesParams,
        requestOptions: RequestOptions,
    ): List<ScalarDocListGuidesResponse> =
        // get /v1/guides
        withRawResponse().listGuides(params, requestOptions).parse()

    override fun publishGuide(
        params: ScalarDocPublishGuideParams,
        requestOptions: RequestOptions,
    ): ScalarDocPublishGuideResponse =
        // post /v1/guides/{slug}/publish
        withRawResponse().publishGuide(params, requestOptions).parse()

    class WithRawResponseImpl internal constructor(private val clientOptions: ClientOptions) :
        ScalarDocService.WithRawResponse {

        private val errorHandler: Handler<HttpResponse> =
            errorHandler(errorBodyHandler(clientOptions.jsonMapper))

        override fun withOptions(
            modifier: Consumer<ClientOptions.Builder>
        ): ScalarDocService.WithRawResponse =
            ScalarDocServiceImpl.WithRawResponseImpl(
                clientOptions.toBuilder().apply(modifier::accept).build()
            )

        private val createGuideHandler: Handler<ScalarDocCreateGuideResponse> =
            jsonHandler<ScalarDocCreateGuideResponse>(clientOptions.jsonMapper)

        override fun createGuide(
            params: ScalarDocCreateGuideParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<ScalarDocCreateGuideResponse> {
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.POST)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "guides")
                    .body(json(clientOptions.jsonMapper, params._body()))
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response
                    .use { createGuideHandler.handle(it) }
                    .also {
                        if (requestOptions.responseValidation!!) {
                            it.validate()
                        }
                    }
            }
        }

        private val listGuidesHandler: Handler<List<ScalarDocListGuidesResponse>> =
            jsonHandler<List<ScalarDocListGuidesResponse>>(clientOptions.jsonMapper)

        override fun listGuides(
            params: ScalarDocListGuidesParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<List<ScalarDocListGuidesResponse>> {
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.GET)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "guides")
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response
                    .use { listGuidesHandler.handle(it) }
                    .also {
                        if (requestOptions.responseValidation!!) {
                            it.forEach { it.validate() }
                        }
                    }
            }
        }

        private val publishGuideHandler: Handler<ScalarDocPublishGuideResponse> =
            jsonHandler<ScalarDocPublishGuideResponse>(clientOptions.jsonMapper)

        override fun publishGuide(
            params: ScalarDocPublishGuideParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<ScalarDocPublishGuideResponse> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("slug", params.slug().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.POST)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "guides", params._pathParam(0), "publish")
                    .apply { params._body().ifPresent { body(json(clientOptions.jsonMapper, it)) } }
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response
                    .use { publishGuideHandler.handle(it) }
                    .also {
                        if (requestOptions.responseValidation!!) {
                            it.validate()
                        }
                    }
            }
        }
    }
}
