// File generated by Scalar SDK Generator. DO NOT EDIT.

package com.scalar.client

import com.scalar.core.ClientOptions
import com.scalar.services.blocking.AuthenticationService
import com.scalar.services.blocking.LoginPortalService
import com.scalar.services.blocking.NamespaceService
import com.scalar.services.blocking.RegistryService
import com.scalar.services.blocking.RuleService
import com.scalar.services.blocking.ScalarDocService
import com.scalar.services.blocking.SchemaService
import com.scalar.services.blocking.TeamService
import com.scalar.services.blocking.ThemeService
import java.util.function.Consumer

/**
 * A client for interacting with the Scalar REST API synchronously. You can also switch to
 * asynchronous execution via the [async] method.
 *
 * This client performs best when you create a single instance and reuse it for all interactions
 * with the REST API. This is because each client holds its own connection pool and thread pools.
 * Reusing connections and threads reduces latency and saves memory. The client also handles rate
 * limiting per client. This means that creating and using multiple instances at the same time will
 * not respect rate limits.
 *
 * The threads and connections that are held will be released automatically if they remain idle. But
 * if you are writing an application that needs to aggressively release unused resources, then you
 * may call [close].
 */
interface ScalarClient {

    /**
     * Returns a version of this client that uses asynchronous execution.
     *
     * The returned client shares its resources, like its connection pool and thread pools, with
     * this client.
     */
    fun async(): ScalarClientAsync

    /**
     * Returns a view of this service that provides access to raw HTTP responses for each method.
     */
    fun withRawResponse(): WithRawResponse

    /**
     * Returns a view of this service with the given option modifications applied.
     *
     * The original service is not modified.
     */
    fun withOptions(modifier: Consumer<ClientOptions.Builder>): ScalarClient

    /** Registry */
    fun registry(): RegistryService

    /** Schemas */
    fun schemas(): SchemaService

    /** Login Portals */
    fun loginPortals(): LoginPortalService

    /** Rules */
    fun rules(): RuleService

    /** Themes */
    fun themes(): ThemeService

    /** Teams */
    fun teams(): TeamService

    /** Scalar Docs */
    fun scalarDocs(): ScalarDocService

    /** Namespaces */
    fun namespaces(): NamespaceService

    /** Authentication */
    fun authentication(): AuthenticationService

    /**
     * Closes this client, relinquishing any underlying resources.
     *
     * This is purposefully not inherited from [AutoCloseable] because the client is long-lived and
     * usually should not be synchronously closed via try-with-resources.
     *
     * It's also usually not necessary to call this method at all. the default HTTP client
     * automatically releases threads and connections if they remain idle, but if you are writing an
     * application that needs to aggressively release unused resources, then you may call this
     * method.
     */
    fun close()

    /** A view of [ScalarClient] that provides access to raw HTTP responses for each method. */
    interface WithRawResponse {

        /**
         * Returns a view of this service with the given option modifications applied.
         *
         * The original service is not modified.
         */
        fun withOptions(modifier: Consumer<ClientOptions.Builder>): ScalarClient.WithRawResponse

        /** Registry */
        fun registry(): RegistryService.WithRawResponse

        /** Schemas */
        fun schemas(): SchemaService.WithRawResponse

        /** Login Portals */
        fun loginPortals(): LoginPortalService.WithRawResponse

        /** Rules */
        fun rules(): RuleService.WithRawResponse

        /** Themes */
        fun themes(): ThemeService.WithRawResponse

        /** Teams */
        fun teams(): TeamService.WithRawResponse

        /** Scalar Docs */
        fun scalarDocs(): ScalarDocService.WithRawResponse

        /** Namespaces */
        fun namespaces(): NamespaceService.WithRawResponse

        /** Authentication */
        fun authentication(): AuthenticationService.WithRawResponse
    }
}
