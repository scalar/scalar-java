// File generated by Scalar SDK Generator. DO NOT EDIT.

package com.scalar.models.authentication

import com.fasterxml.jackson.annotation.JsonAnyGetter
import com.fasterxml.jackson.annotation.JsonAnySetter
import com.fasterxml.jackson.annotation.JsonCreator
import com.fasterxml.jackson.annotation.JsonProperty
import com.scalar.core.ExcludeMissing
import com.scalar.core.JsonField
import com.scalar.core.JsonMissing
import com.scalar.core.JsonValue
import com.scalar.core.checkKnown
import com.scalar.core.checkRequired
import com.scalar.core.toImmutable
import com.scalar.errors.ScalarInvalidDataException
import java.util.Collections
import java.util.Objects
import java.util.Optional
import kotlin.jvm.optionals.getOrNull

class AuthenticationListCurrentUserResponse
@JsonCreator(mode = JsonCreator.Mode.DISABLED)
private constructor(
    private val uid: JsonField<String>,
    private val createdAt: JsonField<Long>,
    private val updatedAt: JsonField<Long>,
    private val email: JsonField<String>,
    private val theme: JsonField<String>,
    private val activeTeamId: JsonField<String>,
    private val hasGithub: JsonField<Boolean>,
    private val teams: JsonField<List<Team>>,
    private val additionalProperties: MutableMap<String, JsonValue>,
) {

    @JsonCreator
    private constructor(
        @JsonProperty("uid") @ExcludeMissing uid: JsonField<String> = JsonMissing.of(),
        @JsonProperty("createdAt") @ExcludeMissing createdAt: JsonField<Long> = JsonMissing.of(),
        @JsonProperty("updatedAt") @ExcludeMissing updatedAt: JsonField<Long> = JsonMissing.of(),
        @JsonProperty("email") @ExcludeMissing email: JsonField<String> = JsonMissing.of(),
        @JsonProperty("theme") @ExcludeMissing theme: JsonField<String> = JsonMissing.of(),
        @JsonProperty("activeTeamId")
        @ExcludeMissing
        activeTeamId: JsonField<String> = JsonMissing.of(),
        @JsonProperty("hasGithub") @ExcludeMissing hasGithub: JsonField<Boolean> = JsonMissing.of(),
        @JsonProperty("teams") @ExcludeMissing teams: JsonField<List<Team>> = JsonMissing.of(),
    ) : this(
        uid,
        createdAt,
        updatedAt,
        email,
        theme,
        activeTeamId,
        hasGithub,
        teams,
        mutableMapOf(),
    )

    /**
     * @throws ScalarInvalidDataException if the JSON field has an unexpected type or is
     *   unexpectedly missing or null (e.g. if the server responded with an unexpected value).
     */
    fun uid(): String = uid.getRequired("uid")

    /**
     * @throws ScalarInvalidDataException if the JSON field has an unexpected type or is
     *   unexpectedly missing or null (e.g. if the server responded with an unexpected value).
     */
    fun createdAt(): Long = createdAt.getRequired("createdAt")

    /**
     * @throws ScalarInvalidDataException if the JSON field has an unexpected type or is
     *   unexpectedly missing or null (e.g. if the server responded with an unexpected value).
     */
    fun updatedAt(): Long = updatedAt.getRequired("updatedAt")

    /**
     * @throws ScalarInvalidDataException if the JSON field has an unexpected type or is
     *   unexpectedly missing or null (e.g. if the server responded with an unexpected value).
     */
    fun email(): String = email.getRequired("email")

    /**
     * @throws ScalarInvalidDataException if the JSON field has an unexpected type (e.g. if the
     *   server responded with an unexpected value).
     */
    fun theme(): Optional<String> = theme.getOptional("theme")

    /**
     * @throws ScalarInvalidDataException if the JSON field has an unexpected type (e.g. if the
     *   server responded with an unexpected value).
     */
    fun activeTeamId(): Optional<String> = activeTeamId.getOptional("activeTeamId")

    /**
     * @throws ScalarInvalidDataException if the JSON field has an unexpected type or is
     *   unexpectedly missing or null (e.g. if the server responded with an unexpected value).
     */
    fun hasGithub(): Boolean = hasGithub.getRequired("hasGithub")

    /**
     * @throws ScalarInvalidDataException if the JSON field has an unexpected type or is
     *   unexpectedly missing or null (e.g. if the server responded with an unexpected value).
     */
    fun teams(): List<Team> = teams.getRequired("teams")

    /**
     * Returns the raw JSON value of [uid].
     *
     * Unlike [uid], this method doesn't throw if the JSON field has an unexpected type.
     */
    @JsonProperty("uid") @ExcludeMissing fun _uid(): JsonField<String> = uid

    /**
     * Returns the raw JSON value of [createdAt].
     *
     * Unlike [createdAt], this method doesn't throw if the JSON field has an unexpected type.
     */
    @JsonProperty("createdAt") @ExcludeMissing fun _createdAt(): JsonField<Long> = createdAt

    /**
     * Returns the raw JSON value of [updatedAt].
     *
     * Unlike [updatedAt], this method doesn't throw if the JSON field has an unexpected type.
     */
    @JsonProperty("updatedAt") @ExcludeMissing fun _updatedAt(): JsonField<Long> = updatedAt

    /**
     * Returns the raw JSON value of [email].
     *
     * Unlike [email], this method doesn't throw if the JSON field has an unexpected type.
     */
    @JsonProperty("email") @ExcludeMissing fun _email(): JsonField<String> = email

    /**
     * Returns the raw JSON value of [theme].
     *
     * Unlike [theme], this method doesn't throw if the JSON field has an unexpected type.
     */
    @JsonProperty("theme") @ExcludeMissing fun _theme(): JsonField<String> = theme

    /**
     * Returns the raw JSON value of [activeTeamId].
     *
     * Unlike [activeTeamId], this method doesn't throw if the JSON field has an unexpected type.
     */
    @JsonProperty("activeTeamId")
    @ExcludeMissing
    fun _activeTeamId(): JsonField<String> = activeTeamId

    /**
     * Returns the raw JSON value of [hasGithub].
     *
     * Unlike [hasGithub], this method doesn't throw if the JSON field has an unexpected type.
     */
    @JsonProperty("hasGithub") @ExcludeMissing fun _hasGithub(): JsonField<Boolean> = hasGithub

    /**
     * Returns the raw JSON value of [teams].
     *
     * Unlike [teams], this method doesn't throw if the JSON field has an unexpected type.
     */
    @JsonProperty("teams") @ExcludeMissing fun _teams(): JsonField<List<Team>> = teams

    @JsonAnySetter
    private fun putAdditionalProperty(key: String, value: JsonValue) {
        additionalProperties.put(key, value)
    }

    @JsonAnyGetter
    @ExcludeMissing
    fun _additionalProperties(): Map<String, JsonValue> =
        Collections.unmodifiableMap(additionalProperties)

    fun toBuilder() = Builder().from(this)

    companion object {

        /**
         * Returns a mutable builder for constructing an instance of
         * [AuthenticationListCurrentUserResponse].
         *
         * The following fields are required:
         * ```kotlin
         * .uid()
         * .createdAt()
         * .updatedAt()
         * .email()
         * .activeTeamId()
         * .hasGithub()
         * .teams()
         * ```
         */
        @JvmStatic fun builder() = Builder()
    }

    /** A builder for [AuthenticationListCurrentUserResponse]. */
    class Builder internal constructor() {

        private var uid: JsonField<String>? = null
        private var createdAt: JsonField<Long>? = null
        private var updatedAt: JsonField<Long>? = null
        private var email: JsonField<String>? = null
        private var theme: JsonField<String> = JsonMissing.of()
        private var activeTeamId: JsonField<String>? = null
        private var hasGithub: JsonField<Boolean>? = null
        private var teams: JsonField<MutableList<Team>>? = null
        private var additionalProperties: MutableMap<String, JsonValue> = mutableMapOf()

        @JvmSynthetic
        internal fun from(
            authenticationListCurrentUserResponse: AuthenticationListCurrentUserResponse
        ) = apply {
            uid = authenticationListCurrentUserResponse.uid
            createdAt = authenticationListCurrentUserResponse.createdAt
            updatedAt = authenticationListCurrentUserResponse.updatedAt
            email = authenticationListCurrentUserResponse.email
            theme = authenticationListCurrentUserResponse.theme
            activeTeamId = authenticationListCurrentUserResponse.activeTeamId
            hasGithub = authenticationListCurrentUserResponse.hasGithub
            teams = authenticationListCurrentUserResponse.teams.map { it.toMutableList() }
            additionalProperties =
                authenticationListCurrentUserResponse.additionalProperties.toMutableMap()
        }

        fun uid(uid: String) = uid(JsonField.of(uid))

        /**
         * Sets [Builder.uid] to an arbitrary JSON value.
         *
         * You should usually call [Builder.uid] with a well-typed [String] value instead. This
         * method is primarily for setting the field to an undocumented or not yet supported value.
         */
        fun uid(uid: JsonField<String>) = apply { this.uid = uid }

        fun createdAt(createdAt: Long) = createdAt(JsonField.of(createdAt))

        /**
         * Sets [Builder.createdAt] to an arbitrary JSON value.
         *
         * You should usually call [Builder.createdAt] with a well-typed [Long] value instead. This
         * method is primarily for setting the field to an undocumented or not yet supported value.
         */
        fun createdAt(createdAt: JsonField<Long>) = apply { this.createdAt = createdAt }

        fun updatedAt(updatedAt: Long) = updatedAt(JsonField.of(updatedAt))

        /**
         * Sets [Builder.updatedAt] to an arbitrary JSON value.
         *
         * You should usually call [Builder.updatedAt] with a well-typed [Long] value instead. This
         * method is primarily for setting the field to an undocumented or not yet supported value.
         */
        fun updatedAt(updatedAt: JsonField<Long>) = apply { this.updatedAt = updatedAt }

        fun email(email: String) = email(JsonField.of(email))

        /**
         * Sets [Builder.email] to an arbitrary JSON value.
         *
         * You should usually call [Builder.email] with a well-typed [String] value instead. This
         * method is primarily for setting the field to an undocumented or not yet supported value.
         */
        fun email(email: JsonField<String>) = apply { this.email = email }

        fun theme(theme: String) = theme(JsonField.of(theme))

        /**
         * Sets [Builder.theme] to an arbitrary JSON value.
         *
         * You should usually call [Builder.theme] with a well-typed [String] value instead. This
         * method is primarily for setting the field to an undocumented or not yet supported value.
         */
        fun theme(theme: JsonField<String>) = apply { this.theme = theme }

        fun activeTeamId(activeTeamId: String?) = activeTeamId(JsonField.ofNullable(activeTeamId))

        /** Alias for calling [Builder.activeTeamId] with `activeTeamId.orElse(null)`. */
        fun activeTeamId(activeTeamId: Optional<String>) = activeTeamId(activeTeamId.getOrNull())

        /**
         * Sets [Builder.activeTeamId] to an arbitrary JSON value.
         *
         * You should usually call [Builder.activeTeamId] with a well-typed [String] value instead.
         * This method is primarily for setting the field to an undocumented or not yet supported
         * value.
         */
        fun activeTeamId(activeTeamId: JsonField<String>) = apply {
            this.activeTeamId = activeTeamId
        }

        fun hasGithub(hasGithub: Boolean) = hasGithub(JsonField.of(hasGithub))

        /**
         * Sets [Builder.hasGithub] to an arbitrary JSON value.
         *
         * You should usually call [Builder.hasGithub] with a well-typed [Boolean] value instead.
         * This method is primarily for setting the field to an undocumented or not yet supported
         * value.
         */
        fun hasGithub(hasGithub: JsonField<Boolean>) = apply { this.hasGithub = hasGithub }

        fun teams(teams: List<Team>) = teams(JsonField.of(teams))

        /**
         * Sets [Builder.teams] to an arbitrary JSON value.
         *
         * You should usually call [Builder.teams] with a well-typed `List<Team>` value instead.
         * This method is primarily for setting the field to an undocumented or not yet supported
         * value.
         */
        fun teams(teams: JsonField<List<Team>>) = apply {
            this.teams = teams.map { it.toMutableList() }
        }

        /**
         * Adds a single [Team] to [teams].
         *
         * @throws IllegalStateException if the field was previously set to a non-list.
         */
        fun addTeam(team: Team) = apply {
            teams =
                (teams ?: JsonField.of(mutableListOf())).also { checkKnown("teams", it).add(team) }
        }

        fun additionalProperties(additionalProperties: Map<String, JsonValue>) = apply {
            this.additionalProperties.clear()
            putAllAdditionalProperties(additionalProperties)
        }

        fun putAdditionalProperty(key: String, value: JsonValue) = apply {
            additionalProperties.put(key, value)
        }

        fun putAllAdditionalProperties(additionalProperties: Map<String, JsonValue>) = apply {
            this.additionalProperties.putAll(additionalProperties)
        }

        fun removeAdditionalProperty(key: String) = apply { additionalProperties.remove(key) }

        fun removeAllAdditionalProperties(keys: Set<String>) = apply {
            keys.forEach(::removeAdditionalProperty)
        }

        /**
         * Returns an immutable instance of [AuthenticationListCurrentUserResponse].
         *
         * Further updates to this [Builder] will not mutate the returned instance.
         *
         * The following fields are required:
         * ```kotlin
         * .uid()
         * .createdAt()
         * .updatedAt()
         * .email()
         * .activeTeamId()
         * .hasGithub()
         * .teams()
         * ```
         *
         * @throws IllegalStateException if any required field is unset.
         */
        fun build(): AuthenticationListCurrentUserResponse =
            AuthenticationListCurrentUserResponse(
                checkRequired("uid", uid),
                checkRequired("createdAt", createdAt),
                checkRequired("updatedAt", updatedAt),
                checkRequired("email", email),
                theme,
                checkRequired("activeTeamId", activeTeamId),
                checkRequired("hasGithub", hasGithub),
                checkRequired("teams", teams).map { it.toImmutable() },
                additionalProperties.toMutableMap(),
            )
    }

    private var validated: Boolean = false

    /**
     * Validates that the types of all values in this object match their expected types recursively.
     *
     * This method is _not_ forwards compatible with new types from the API for existing fields.
     *
     * @throws ScalarInvalidDataException if any value type in this object doesn't match its
     *   expected type.
     */
    fun validate(): AuthenticationListCurrentUserResponse = apply {
        if (validated) {
            return@apply
        }

        uid()
        createdAt()
        updatedAt()
        email()
        theme()
        activeTeamId()
        hasGithub()
        teams().forEach { it.validate() }
        validated = true
    }

    fun isValid(): Boolean =
        try {
            validate()
            true
        } catch (e: ScalarInvalidDataException) {
            false
        }

    /**
     * Returns a score indicating how many valid values are contained in this object recursively.
     *
     * Used for best match union deserialization.
     */
    @JvmSynthetic
    internal fun validity(): Int =
        (if (uid.asKnown().isPresent) 1 else 0) +
            (if (createdAt.asKnown().isPresent) 1 else 0) +
            (if (updatedAt.asKnown().isPresent) 1 else 0) +
            (if (email.asKnown().isPresent) 1 else 0) +
            (if (theme.asKnown().isPresent) 1 else 0) +
            (if (activeTeamId.asKnown().isPresent) 1 else 0) +
            (if (hasGithub.asKnown().isPresent) 1 else 0) +
            (teams.asKnown().getOrNull()?.sumOf { it.validity().toInt() } ?: 0)

    class Team
    @JsonCreator(mode = JsonCreator.Mode.DISABLED)
    private constructor(
        private val uid: JsonField<String>,
        private val name: JsonField<String>,
        private val imageUri: JsonField<String>,
        private val additionalProperties: MutableMap<String, JsonValue>,
    ) {

        @JsonCreator
        private constructor(
            @JsonProperty("uid") @ExcludeMissing uid: JsonField<String> = JsonMissing.of(),
            @JsonProperty("name") @ExcludeMissing name: JsonField<String> = JsonMissing.of(),
            @JsonProperty("imageUri")
            @ExcludeMissing
            imageUri: JsonField<String> = JsonMissing.of(),
        ) : this(uid, name, imageUri, mutableMapOf())

        /**
         * @throws ScalarInvalidDataException if the JSON field has an unexpected type or is
         *   unexpectedly missing or null (e.g. if the server responded with an unexpected value).
         */
        fun uid(): String = uid.getRequired("uid")

        /**
         * @throws ScalarInvalidDataException if the JSON field has an unexpected type or is
         *   unexpectedly missing or null (e.g. if the server responded with an unexpected value).
         */
        fun name(): String = name.getRequired("name")

        /**
         * @throws ScalarInvalidDataException if the JSON field has an unexpected type (e.g. if the
         *   server responded with an unexpected value).
         */
        fun imageUri(): Optional<String> = imageUri.getOptional("imageUri")

        /**
         * Returns the raw JSON value of [uid].
         *
         * Unlike [uid], this method doesn't throw if the JSON field has an unexpected type.
         */
        @JsonProperty("uid") @ExcludeMissing fun _uid(): JsonField<String> = uid

        /**
         * Returns the raw JSON value of [name].
         *
         * Unlike [name], this method doesn't throw if the JSON field has an unexpected type.
         */
        @JsonProperty("name") @ExcludeMissing fun _name(): JsonField<String> = name

        /**
         * Returns the raw JSON value of [imageUri].
         *
         * Unlike [imageUri], this method doesn't throw if the JSON field has an unexpected type.
         */
        @JsonProperty("imageUri") @ExcludeMissing fun _imageUri(): JsonField<String> = imageUri

        @JsonAnySetter
        private fun putAdditionalProperty(key: String, value: JsonValue) {
            additionalProperties.put(key, value)
        }

        @JsonAnyGetter
        @ExcludeMissing
        fun _additionalProperties(): Map<String, JsonValue> =
            Collections.unmodifiableMap(additionalProperties)

        fun toBuilder() = Builder().from(this)

        companion object {

            /**
             * Returns a mutable builder for constructing an instance of [Team].
             *
             * The following fields are required:
             * ```kotlin
             * .uid()
             * .name()
             * ```
             */
            @JvmStatic fun builder() = Builder()
        }

        /** A builder for [Team]. */
        class Builder internal constructor() {

            private var uid: JsonField<String>? = null
            private var name: JsonField<String>? = null
            private var imageUri: JsonField<String> = JsonMissing.of()
            private var additionalProperties: MutableMap<String, JsonValue> = mutableMapOf()

            @JvmSynthetic
            internal fun from(team: Team) = apply {
                uid = team.uid
                name = team.name
                imageUri = team.imageUri
                additionalProperties = team.additionalProperties.toMutableMap()
            }

            fun uid(uid: String) = uid(JsonField.of(uid))

            /**
             * Sets [Builder.uid] to an arbitrary JSON value.
             *
             * You should usually call [Builder.uid] with a well-typed [String] value instead. This
             * method is primarily for setting the field to an undocumented or not yet supported
             * value.
             */
            fun uid(uid: JsonField<String>) = apply { this.uid = uid }

            fun name(name: String) = name(JsonField.of(name))

            /**
             * Sets [Builder.name] to an arbitrary JSON value.
             *
             * You should usually call [Builder.name] with a well-typed [String] value instead. This
             * method is primarily for setting the field to an undocumented or not yet supported
             * value.
             */
            fun name(name: JsonField<String>) = apply { this.name = name }

            fun imageUri(imageUri: String) = imageUri(JsonField.of(imageUri))

            /**
             * Sets [Builder.imageUri] to an arbitrary JSON value.
             *
             * You should usually call [Builder.imageUri] with a well-typed [String] value instead.
             * This method is primarily for setting the field to an undocumented or not yet
             * supported value.
             */
            fun imageUri(imageUri: JsonField<String>) = apply { this.imageUri = imageUri }

            fun additionalProperties(additionalProperties: Map<String, JsonValue>) = apply {
                this.additionalProperties.clear()
                putAllAdditionalProperties(additionalProperties)
            }

            fun putAdditionalProperty(key: String, value: JsonValue) = apply {
                additionalProperties.put(key, value)
            }

            fun putAllAdditionalProperties(additionalProperties: Map<String, JsonValue>) = apply {
                this.additionalProperties.putAll(additionalProperties)
            }

            fun removeAdditionalProperty(key: String) = apply { additionalProperties.remove(key) }

            fun removeAllAdditionalProperties(keys: Set<String>) = apply {
                keys.forEach(::removeAdditionalProperty)
            }

            /**
             * Returns an immutable instance of [Team].
             *
             * Further updates to this [Builder] will not mutate the returned instance.
             *
             * The following fields are required:
             * ```kotlin
             * .uid()
             * .name()
             * ```
             *
             * @throws IllegalStateException if any required field is unset.
             */
            fun build(): Team =
                Team(
                    checkRequired("uid", uid),
                    checkRequired("name", name),
                    imageUri,
                    additionalProperties.toMutableMap(),
                )
        }

        private var validated: Boolean = false

        /**
         * Validates that the types of all values in this object match their expected types
         * recursively.
         *
         * This method is _not_ forwards compatible with new types from the API for existing fields.
         *
         * @throws ScalarInvalidDataException if any value type in this object doesn't match its
         *   expected type.
         */
        fun validate(): Team = apply {
            if (validated) {
                return@apply
            }

            uid()
            name()
            imageUri()
            validated = true
        }

        fun isValid(): Boolean =
            try {
                validate()
                true
            } catch (e: ScalarInvalidDataException) {
                false
            }

        /**
         * Returns a score indicating how many valid values are contained in this object
         * recursively.
         *
         * Used for best match union deserialization.
         */
        @JvmSynthetic
        internal fun validity(): Int =
            (if (uid.asKnown().isPresent) 1 else 0) +
                (if (name.asKnown().isPresent) 1 else 0) +
                (if (imageUri.asKnown().isPresent) 1 else 0)

        override fun equals(other: Any?): Boolean {
            if (this === other) {
                return true
            }

            return other is Team &&
                uid == other.uid &&
                name == other.name &&
                imageUri == other.imageUri &&
                additionalProperties == other.additionalProperties
        }

        private val hashCode: Int by lazy {
            Objects.hash(uid, name, imageUri, additionalProperties)
        }

        override fun hashCode(): Int = hashCode

        override fun toString() =
            "Team{uid=$uid, name=$name, imageUri=$imageUri, additionalProperties=$additionalProperties}"
    }

    override fun equals(other: Any?): Boolean {
        if (this === other) {
            return true
        }

        return other is AuthenticationListCurrentUserResponse &&
            uid == other.uid &&
            createdAt == other.createdAt &&
            updatedAt == other.updatedAt &&
            email == other.email &&
            theme == other.theme &&
            activeTeamId == other.activeTeamId &&
            hasGithub == other.hasGithub &&
            teams == other.teams &&
            additionalProperties == other.additionalProperties
    }

    private val hashCode: Int by lazy {
        Objects.hash(
            uid,
            createdAt,
            updatedAt,
            email,
            theme,
            activeTeamId,
            hasGithub,
            teams,
            additionalProperties,
        )
    }

    override fun hashCode(): Int = hashCode

    override fun toString() =
        "AuthenticationListCurrentUserResponse{uid=$uid, createdAt=$createdAt, updatedAt=$updatedAt, email=$email, theme=$theme, activeTeamId=$activeTeamId, hasGithub=$hasGithub, teams=$teams, additionalProperties=$additionalProperties}"
}
