// File generated by Scalar SDK Generator. DO NOT EDIT.

package com.scalar.services.blocking

import com.scalar.core.ClientOptions
import com.scalar.core.RequestOptions
import com.scalar.core.checkRequired
import com.scalar.core.handlers.errorBodyHandler
import com.scalar.core.handlers.errorHandler
import com.scalar.core.handlers.jsonHandler
import com.scalar.core.http.HttpMethod
import com.scalar.core.http.HttpRequest
import com.scalar.core.http.HttpResponse
import com.scalar.core.http.HttpResponse.Handler
import com.scalar.core.http.HttpResponseFor
import com.scalar.core.http.json
import com.scalar.core.http.parseable
import com.scalar.core.prepare
import com.scalar.models.Uid
import com.scalar.models.loginPortals.LoginPortalCreateParams
import com.scalar.models.loginPortals.LoginPortalDeleteParams
import com.scalar.models.loginPortals.LoginPortalListParams
import com.scalar.models.loginPortals.LoginPortalListResponse
import com.scalar.models.loginPortals.LoginPortalRetrieveParams
import com.scalar.models.loginPortals.LoginPortalRetrieveResponse
import com.scalar.models.loginPortals.LoginPortalUpdateParams
import java.util.function.Consumer
import kotlin.jvm.optionals.getOrNull

/** Login Portals */
class LoginPortalServiceImpl internal constructor(private val clientOptions: ClientOptions) :
    LoginPortalService {

    private val withRawResponse: LoginPortalService.WithRawResponse by lazy {
        WithRawResponseImpl(clientOptions)
    }

    override fun withRawResponse(): LoginPortalService.WithRawResponse = withRawResponse

    override fun withOptions(modifier: Consumer<ClientOptions.Builder>): LoginPortalService =
        LoginPortalServiceImpl(clientOptions.toBuilder().apply(modifier::accept).build())

    override fun create(params: LoginPortalCreateParams, requestOptions: RequestOptions): Uid =
        // post /v1/login-portals
        withRawResponse().create(params, requestOptions).parse()

    override fun retrieve(
        params: LoginPortalRetrieveParams,
        requestOptions: RequestOptions,
    ): LoginPortalRetrieveResponse =
        // get /v1/login-portals/{slug}
        withRawResponse().retrieve(params, requestOptions).parse()

    override fun update(params: LoginPortalUpdateParams, requestOptions: RequestOptions): Any? =
        // patch /v1/login-portals/{slug}
        withRawResponse().update(params, requestOptions).parse()

    override fun list(
        params: LoginPortalListParams,
        requestOptions: RequestOptions,
    ): List<LoginPortalListResponse> =
        // get /v1/login-portals
        withRawResponse().list(params, requestOptions).parse()

    override fun delete(params: LoginPortalDeleteParams, requestOptions: RequestOptions): Any? =
        // delete /v1/login-portals/{slug}
        withRawResponse().delete(params, requestOptions).parse()

    class WithRawResponseImpl internal constructor(private val clientOptions: ClientOptions) :
        LoginPortalService.WithRawResponse {

        private val errorHandler: Handler<HttpResponse> =
            errorHandler(errorBodyHandler(clientOptions.jsonMapper))

        override fun withOptions(
            modifier: Consumer<ClientOptions.Builder>
        ): LoginPortalService.WithRawResponse =
            LoginPortalServiceImpl.WithRawResponseImpl(
                clientOptions.toBuilder().apply(modifier::accept).build()
            )

        private val createHandler: Handler<Uid> = jsonHandler<Uid>(clientOptions.jsonMapper)

        override fun create(
            params: LoginPortalCreateParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<Uid> {
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.POST)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "login-portals")
                    .body(json(clientOptions.jsonMapper, params._body()))
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response
                    .use { createHandler.handle(it) }
                    .also {
                        if (requestOptions.responseValidation!!) {
                            it.validate()
                        }
                    }
            }
        }

        private val retrieveHandler: Handler<LoginPortalRetrieveResponse> =
            jsonHandler<LoginPortalRetrieveResponse>(clientOptions.jsonMapper)

        override fun retrieve(
            params: LoginPortalRetrieveParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<LoginPortalRetrieveResponse> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("slug", params.slug().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.GET)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "login-portals", params._pathParam(0))
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response
                    .use { retrieveHandler.handle(it) }
                    .also {
                        if (requestOptions.responseValidation!!) {
                            it.validate()
                        }
                    }
            }
        }

        private val updateHandler: Handler<Any?> = jsonHandler<Any?>(clientOptions.jsonMapper)

        override fun update(
            params: LoginPortalUpdateParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<Any?> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("slug", params.slug().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.PATCH)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "login-portals", params._pathParam(0))
                    .body(json(clientOptions.jsonMapper, params._body()))
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response.use { updateHandler.handle(it) }
            }
        }

        private val listHandler: Handler<List<LoginPortalListResponse>> =
            jsonHandler<List<LoginPortalListResponse>>(clientOptions.jsonMapper)

        override fun list(
            params: LoginPortalListParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<List<LoginPortalListResponse>> {
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.GET)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "login-portals")
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response
                    .use { listHandler.handle(it) }
                    .also {
                        if (requestOptions.responseValidation!!) {
                            it.forEach { it.validate() }
                        }
                    }
            }
        }

        private val deleteHandler: Handler<Any?> = jsonHandler<Any?>(clientOptions.jsonMapper)

        override fun delete(
            params: LoginPortalDeleteParams,
            requestOptions: RequestOptions,
        ): HttpResponseFor<Any?> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("slug", params.slug().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.DELETE)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "login-portals", params._pathParam(0))
                    .apply { params._body().ifPresent { body(json(clientOptions.jsonMapper, it)) } }
                    .build()
                    .prepare(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            val response = clientOptions.httpClient.execute(request, requestOptions)
            return errorHandler.handle(response).parseable {
                response.use { deleteHandler.handle(it) }
            }
        }
    }
}
