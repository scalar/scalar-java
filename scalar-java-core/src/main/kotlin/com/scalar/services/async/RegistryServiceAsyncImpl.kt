// File generated by Scalar SDK Generator. DO NOT EDIT.

package com.scalar.services.async

import com.scalar.core.ClientOptions
import com.scalar.core.RequestOptions
import com.scalar.core.checkRequired
import com.scalar.core.handlers.errorBodyHandler
import com.scalar.core.handlers.errorHandler
import com.scalar.core.handlers.jsonHandler
import com.scalar.core.handlers.stringHandler
import com.scalar.core.http.HttpMethod
import com.scalar.core.http.HttpRequest
import com.scalar.core.http.HttpResponse
import com.scalar.core.http.HttpResponse.Handler
import com.scalar.core.http.HttpResponseFor
import com.scalar.core.http.json
import com.scalar.core.http.parseable
import com.scalar.core.prepareAsync
import com.scalar.models.ManagedDocVersion
import com.scalar.models.registry.RegistryCreateApiDocumentAccessGroupParams
import com.scalar.models.registry.RegistryCreateApiDocumentParams
import com.scalar.models.registry.RegistryCreateApiDocumentResponse
import com.scalar.models.registry.RegistryCreateApiDocumentVersionParams
import com.scalar.models.registry.RegistryDeleteApiDocumentAccessGroupParams
import com.scalar.models.registry.RegistryDeleteApiDocumentParams
import com.scalar.models.registry.RegistryDeleteApiDocumentVersionParams
import com.scalar.models.registry.RegistryListAllApiDocumentsParams
import com.scalar.models.registry.RegistryListAllApiDocumentsResponse
import com.scalar.models.registry.RegistryListApiDocumentVersionMetadataParams
import com.scalar.models.registry.RegistryListApiDocumentsParams
import com.scalar.models.registry.RegistryListApiDocumentsResponse
import com.scalar.models.registry.RegistryRetrieveApiDocumentVersionParams
import com.scalar.models.registry.RegistryUpdateApiDocumentParams
import com.scalar.models.registry.RegistryUpdateApiDocumentVersionParams
import com.scalar.models.registry.RegistryUpdateApiDocumentVersionResponse
import java.util.concurrent.CompletableFuture
import java.util.function.Consumer
import kotlin.jvm.optionals.getOrNull

/** Registry */
class RegistryServiceAsyncImpl internal constructor(private val clientOptions: ClientOptions) :
    RegistryServiceAsync {

    private val withRawResponse: RegistryServiceAsync.WithRawResponse by lazy {
        WithRawResponseImpl(clientOptions)
    }

    override fun withRawResponse(): RegistryServiceAsync.WithRawResponse = withRawResponse

    override fun withOptions(modifier: Consumer<ClientOptions.Builder>): RegistryServiceAsync =
        RegistryServiceAsyncImpl(clientOptions.toBuilder().apply(modifier::accept).build())

    override fun createApiDocument(
        params: RegistryCreateApiDocumentParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<RegistryCreateApiDocumentResponse> =
        // post /v1/apis/{namespace}
        withRawResponse().createApiDocument(params, requestOptions).thenApply { it.parse() }

    override fun createApiDocumentAccessGroup(
        params: RegistryCreateApiDocumentAccessGroupParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<Any?> =
        // post /v1/apis/{namespace}/{slug}/access-group
        withRawResponse().createApiDocumentAccessGroup(params, requestOptions).thenApply {
            it.parse()
        }

    override fun createApiDocumentVersion(
        params: RegistryCreateApiDocumentVersionParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<ManagedDocVersion> =
        // post /v1/apis/{namespace}/{slug}/version
        withRawResponse().createApiDocumentVersion(params, requestOptions).thenApply { it.parse() }

    override fun deleteApiDocument(
        params: RegistryDeleteApiDocumentParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<Any?> =
        // delete /v1/apis/{namespace}/{slug}
        withRawResponse().deleteApiDocument(params, requestOptions).thenApply { it.parse() }

    override fun deleteApiDocumentAccessGroup(
        params: RegistryDeleteApiDocumentAccessGroupParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<Any?> =
        // delete /v1/apis/{namespace}/{slug}/access-group
        withRawResponse().deleteApiDocumentAccessGroup(params, requestOptions).thenApply {
            it.parse()
        }

    override fun deleteApiDocumentVersion(
        params: RegistryDeleteApiDocumentVersionParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<Any?> =
        // delete /v1/apis/{namespace}/{slug}/version/{semver}
        withRawResponse().deleteApiDocumentVersion(params, requestOptions).thenApply { it.parse() }

    override fun listAllApiDocuments(
        params: RegistryListAllApiDocumentsParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<List<RegistryListAllApiDocumentsResponse>> =
        // get /v1/apis
        withRawResponse().listAllApiDocuments(params, requestOptions).thenApply { it.parse() }

    override fun listApiDocumentVersionMetadata(
        params: RegistryListApiDocumentVersionMetadataParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<ManagedDocVersion> =
        // get /v1/apis/{namespace}/{slug}/version/{semver}/metadata
        withRawResponse().listApiDocumentVersionMetadata(params, requestOptions).thenApply {
            it.parse()
        }

    override fun listApiDocuments(
        params: RegistryListApiDocumentsParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<List<RegistryListApiDocumentsResponse>> =
        // get /v1/apis/{namespace}
        withRawResponse().listApiDocuments(params, requestOptions).thenApply { it.parse() }

    override fun retrieveApiDocumentVersion(
        params: RegistryRetrieveApiDocumentVersionParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<String> =
        // get /v1/apis/{namespace}/{slug}/version/{semver}
        withRawResponse().retrieveApiDocumentVersion(params, requestOptions).thenApply {
            it.parse()
        }

    override fun updateApiDocument(
        params: RegistryUpdateApiDocumentParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<Any?> =
        // patch /v1/apis/{namespace}/{slug}
        withRawResponse().updateApiDocument(params, requestOptions).thenApply { it.parse() }

    override fun updateApiDocumentVersion(
        params: RegistryUpdateApiDocumentVersionParams,
        requestOptions: RequestOptions,
    ): CompletableFuture<RegistryUpdateApiDocumentVersionResponse> =
        // patch /v1/apis/{namespace}/{slug}/version/{semver}
        withRawResponse().updateApiDocumentVersion(params, requestOptions).thenApply { it.parse() }

    class WithRawResponseImpl internal constructor(private val clientOptions: ClientOptions) :
        RegistryServiceAsync.WithRawResponse {

        private val errorHandler: Handler<HttpResponse> =
            errorHandler(errorBodyHandler(clientOptions.jsonMapper))

        override fun withOptions(
            modifier: Consumer<ClientOptions.Builder>
        ): RegistryServiceAsync.WithRawResponse =
            RegistryServiceAsyncImpl.WithRawResponseImpl(
                clientOptions.toBuilder().apply(modifier::accept).build()
            )

        private val createApiDocumentHandler: Handler<RegistryCreateApiDocumentResponse> =
            jsonHandler<RegistryCreateApiDocumentResponse>(clientOptions.jsonMapper)

        override fun createApiDocument(
            params: RegistryCreateApiDocumentParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<RegistryCreateApiDocumentResponse>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("namespace", params.namespace().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.POST)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "apis", params._pathParam(0))
                    .body(json(clientOptions.jsonMapper, params._body()))
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response
                            .use { createApiDocumentHandler.handle(it) }
                            .also {
                                if (requestOptions.responseValidation!!) {
                                    it.validate()
                                }
                            }
                    }
                }
        }

        private val createApiDocumentAccessGroupHandler: Handler<Any?> =
            jsonHandler<Any?>(clientOptions.jsonMapper)

        override fun createApiDocumentAccessGroup(
            params: RegistryCreateApiDocumentAccessGroupParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<Any?>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("slug", params.slug().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.POST)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments(
                        "v1",
                        "apis",
                        params._pathParam(0),
                        params._pathParam(1),
                        "access-group",
                    )
                    .body(json(clientOptions.jsonMapper, params._body()))
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response.use { createApiDocumentAccessGroupHandler.handle(it) }
                    }
                }
        }

        private val createApiDocumentVersionHandler: Handler<ManagedDocVersion> =
            jsonHandler<ManagedDocVersion>(clientOptions.jsonMapper)

        override fun createApiDocumentVersion(
            params: RegistryCreateApiDocumentVersionParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<ManagedDocVersion>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("slug", params.slug().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.POST)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments(
                        "v1",
                        "apis",
                        params._pathParam(0),
                        params._pathParam(1),
                        "version",
                    )
                    .body(json(clientOptions.jsonMapper, params._body()))
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response
                            .use { createApiDocumentVersionHandler.handle(it) }
                            .also {
                                if (requestOptions.responseValidation!!) {
                                    it.validate()
                                }
                            }
                    }
                }
        }

        private val deleteApiDocumentHandler: Handler<Any?> =
            jsonHandler<Any?>(clientOptions.jsonMapper)

        override fun deleteApiDocument(
            params: RegistryDeleteApiDocumentParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<Any?>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("slug", params.slug().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.DELETE)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "apis", params._pathParam(0), params._pathParam(1))
                    .apply { params._body().ifPresent { body(json(clientOptions.jsonMapper, it)) } }
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response.use { deleteApiDocumentHandler.handle(it) }
                    }
                }
        }

        private val deleteApiDocumentAccessGroupHandler: Handler<Any?> =
            jsonHandler<Any?>(clientOptions.jsonMapper)

        override fun deleteApiDocumentAccessGroup(
            params: RegistryDeleteApiDocumentAccessGroupParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<Any?>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("slug", params.slug().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.DELETE)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments(
                        "v1",
                        "apis",
                        params._pathParam(0),
                        params._pathParam(1),
                        "access-group",
                    )
                    .body(json(clientOptions.jsonMapper, params._body()))
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response.use { deleteApiDocumentAccessGroupHandler.handle(it) }
                    }
                }
        }

        private val deleteApiDocumentVersionHandler: Handler<Any?> =
            jsonHandler<Any?>(clientOptions.jsonMapper)

        override fun deleteApiDocumentVersion(
            params: RegistryDeleteApiDocumentVersionParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<Any?>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("semver", params.semver().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.DELETE)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments(
                        "v1",
                        "apis",
                        params._pathParam(0),
                        params._pathParam(1),
                        "version",
                        params._pathParam(2),
                    )
                    .apply { params._body().ifPresent { body(json(clientOptions.jsonMapper, it)) } }
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response.use { deleteApiDocumentVersionHandler.handle(it) }
                    }
                }
        }

        private val listAllApiDocumentsHandler: Handler<List<RegistryListAllApiDocumentsResponse>> =
            jsonHandler<List<RegistryListAllApiDocumentsResponse>>(clientOptions.jsonMapper)

        override fun listAllApiDocuments(
            params: RegistryListAllApiDocumentsParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<List<RegistryListAllApiDocumentsResponse>>> {
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.GET)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "apis")
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response
                            .use { listAllApiDocumentsHandler.handle(it) }
                            .also {
                                if (requestOptions.responseValidation!!) {
                                    it.forEach { it.validate() }
                                }
                            }
                    }
                }
        }

        private val listApiDocumentVersionMetadataHandler: Handler<ManagedDocVersion> =
            jsonHandler<ManagedDocVersion>(clientOptions.jsonMapper)

        override fun listApiDocumentVersionMetadata(
            params: RegistryListApiDocumentVersionMetadataParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<ManagedDocVersion>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("semver", params.semver().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.GET)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments(
                        "v1",
                        "apis",
                        params._pathParam(0),
                        params._pathParam(1),
                        "version",
                        params._pathParam(2),
                        "metadata",
                    )
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response
                            .use { listApiDocumentVersionMetadataHandler.handle(it) }
                            .also {
                                if (requestOptions.responseValidation!!) {
                                    it.validate()
                                }
                            }
                    }
                }
        }

        private val listApiDocumentsHandler: Handler<List<RegistryListApiDocumentsResponse>> =
            jsonHandler<List<RegistryListApiDocumentsResponse>>(clientOptions.jsonMapper)

        override fun listApiDocuments(
            params: RegistryListApiDocumentsParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<List<RegistryListApiDocumentsResponse>>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("namespace", params.namespace().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.GET)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "apis", params._pathParam(0))
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response
                            .use { listApiDocumentsHandler.handle(it) }
                            .also {
                                if (requestOptions.responseValidation!!) {
                                    it.forEach { it.validate() }
                                }
                            }
                    }
                }
        }

        private val retrieveApiDocumentVersionHandler: Handler<String> = stringHandler()

        override fun retrieveApiDocumentVersion(
            params: RegistryRetrieveApiDocumentVersionParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<String>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("semver", params.semver().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.GET)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments(
                        "v1",
                        "apis",
                        params._pathParam(0),
                        params._pathParam(1),
                        "version",
                        params._pathParam(2),
                    )
                    .putHeader("Accept", "text/plain")
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response.use { retrieveApiDocumentVersionHandler.handle(it) }
                    }
                }
        }

        private val updateApiDocumentHandler: Handler<Any?> =
            jsonHandler<Any?>(clientOptions.jsonMapper)

        override fun updateApiDocument(
            params: RegistryUpdateApiDocumentParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<Any?>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("slug", params.slug().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.PATCH)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments("v1", "apis", params._pathParam(0), params._pathParam(1))
                    .body(json(clientOptions.jsonMapper, params._body()))
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response.use { updateApiDocumentHandler.handle(it) }
                    }
                }
        }

        private val updateApiDocumentVersionHandler:
            Handler<RegistryUpdateApiDocumentVersionResponse> =
            jsonHandler<RegistryUpdateApiDocumentVersionResponse>(clientOptions.jsonMapper)

        override fun updateApiDocumentVersion(
            params: RegistryUpdateApiDocumentVersionParams,
            requestOptions: RequestOptions,
        ): CompletableFuture<HttpResponseFor<RegistryUpdateApiDocumentVersionResponse>> {
            // We check here instead of in the params builder because this can be specified
            // positionally or in the params class.
            checkRequired("semver", params.semver().getOrNull())
            val request =
                HttpRequest.builder()
                    .method(HttpMethod.PATCH)
                    .baseUrl(clientOptions.baseUrl())
                    .addPathSegments(
                        "v1",
                        "apis",
                        params._pathParam(0),
                        params._pathParam(1),
                        "version",
                        params._pathParam(2),
                    )
                    .body(json(clientOptions.jsonMapper, params._body()))
                    .build()
                    .prepareAsync(clientOptions, params)
            val requestOptions = requestOptions.applyDefaults(RequestOptions.from(clientOptions))
            return request
                .thenComposeAsync { clientOptions.httpClient.executeAsync(it, requestOptions) }
                .thenApply { response ->
                    errorHandler.handle(response).parseable {
                        response
                            .use { updateApiDocumentVersionHandler.handle(it) }
                            .also {
                                if (requestOptions.responseValidation!!) {
                                    it.validate()
                                }
                            }
                    }
                }
        }
    }
}
